# --- Source PDFs: too large for git (GitHub rejects files >100MB; these are 100-150MB each) ---
*.pdf

# --- Build intermediates / large preview images (regenerable by build/render.py) ---
build/contact_sheet.png
build/sheet_M*.png

# --- Local test artifacts ---
.playwright-mcp/
/test-*.jpeg
/test-*.png

# --- Local Claude Code config (keep local, never publish to remote) ---
.claude/

# --- OS cruft ---
Thumbs.db
.DS_Store
desktop.ini

# --- Local audit/temp scripts (never publish) ---
build/_*.py

# user-provided source reference image (cover uses high-res ㄆ instead)
/peach.png
